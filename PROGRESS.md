# 진행 상황

> 최종 갱신 2026-09-14. 상세는 [docs/](docs/README.md).

## 현재 위치

```
[완료] 설비 구조 확정 ─── 바이패스 발견·검증, 히터 4대 확정, 도면 레이어 판독법
[완료] 데이터 특성화 ─── 태그 52개, DI 3함정, 없는 데이터 확정
[완료] 관측 레이어   ─── regime(flow/lowflow/isolated) + β, 검증 T1~T4 통과
[완료] 물리모델 재작성 ─ 계열 단위 H1~H6, 식별성 분석
[완료] 무부하 열량계 ─── C_eff·Q_loss 측정. Q_loss = 설계열량의 1.50~2.50% (4대 일관)
[기각] 절대 유량 역산 ── fire(유효 화력률)가 물리 제약으로 안 잡힘. 경로 자체를 버림
[완료] 구조 재설계   ─── U·A = Q_gas·NTU/Δ. 절대 유량·합류점식·부하보정 전부 불필요
[기각] 열화지표      ─── 대조군 검정에서 24개 지표 전부 실패(최소 p=0.465). docs/07
[기각] 알람 선행예측 ─── 알람이 고장이 아님(공정 이탈·자동복구). 516개 검정 유의 4개(동어반복)
[완료] ✅ 빙결 위험도 ─ 6h P(출구가스<0℃). AUC 0.79~0.84, 리프트 17~24배. docs/08, risk/
[완료] ✅ 제한 PINN ─── 성립식(Q5 줄-톰슨·Q6 배관 NTU)만. 정제 KF·AE·EKF + Optuna 8:2·5-fold + XAI. docs/16~18
[완료] ✅ 구조 비교 ─── hard·soft·nn × 타깃(이상값 제거·1시간 평균) × 정제 5 × 시드 5 = 60작업. docs/19
[진행] 학회 포스터 ─── 구성안·설비도·물성치 정리 완료, 포스터용 그림 재생성 남음. docs/20
```

## 무엇이 확정됐나

- **설비**: 두 계열(M = A+B 도시가스 / Z = O+P 공항·발전)이 완전히 분리. 각 계열에
  **히터를 건너뛰는 냉가스 바이패스**가 있고 온도조절기가 헤더온도를 맞춘다.
  가스의 **41%(M)·60%(Z)가 히터를 지나지 않는다.** HTR-31C 는 미설치(계획).
- **헤더 온도 태그 확인**: `TSL_TSH_TSHH-31M/Z` 가 합류점 하류임을 편상관 −0.52/−0.53 으로 확인.
- **관측 레이어**: 열전달 해석이 유효한 `flow` 구간이 히터당 692~2,000일.
- **전처리**: `prex_multi/output/trend_all.parquet` 7,804,924행 × 52열
  (버너상태·β·운전상태 포함, DI 3함정 보정 완료).

## 무엇이 막혀 있나

- **유량계가 데이터에 전혀 없다** (`FI61*`·`FQ61*` 40개 컬럼 전부 NULL).
  → 버너 가동률 열량법으로 역산. `Σm ≤ m_tot` 만족률 96.5%/93.9%.
- **절대 유량은 포기했다.** `fire`(유효 화력률)를 물리 제약으로 좁히려 했으나 "설계 초과 창"
  비율이 매끄럽게 변할 뿐 꺾이는 점이 없었다(A 0.4 · B 0.4 · O 0.35 · P 0.8 로 흩어짐).
- **대신 `U·A = Q_gas·NTU/Δ` 로 재설계했다.** `fire`·`Q_b` 가 시불변 상수라 **시간 비율에서 소거**된다.
  `m_tot`·`f_i`·합류점식·부하보정이 전부 불필요해졌다. 히터 단독으로 닫힌다.
- **A/B/O 는 설계 U·A 가 T3 프록시**(P 도서 유량비 스케일)라 절대 수준을 못 쓴다. 상대 추세만.
- **진짜 고장 라벨이 없다.** SCADA 에도 정기점검 보고서에도 없다.
  → 열화지표 추세로 판정하고, 정비 시점의 φ 회복으로 검증한다.

## 기각된 접근 (다시 시도하지 말 것)

정압기 밸브식 유량 역산 · "긴 OFF/짧은 OFF" 분리 추정 · 잔열 지수감쇠 동시추정 ·
원시 센서에 AE/KF 직접 적용 · **ε-NTU 로 유량 배분 역산**(부호가 반대다 — 문서 10) ·
**(Q1) 수조 열수지를 PINN 물리층에 넣기**(버너 발열이 ε 역산
흡수열의 1/3~1/4 뿐 — 데이터 자체가 모순). 사유는 [06_정정이력.md](docs/06_정정이력.md).

> ⚠ **"ε → 유량 역산"은 기각 목록에서 뺐다** (2026-09-13). 구조적 미식별이라던 근거가
> 틀렸다 — `U` 가 `m` 에 의존하므로 `NTU(x)` 는 단조·역산가능하다. 다만 제작도서 상수를
> 고정한 **순방향** 역산은 계절성 검증에 실패하므로, `U·A` 보정계수를 학습 모수로 두고
> PINN 안에서 푼다. [09_PINN_물리층.md](docs/09_PINN_물리층.md) §3.

## 타임라인

| 날짜 | 내용 |
|---|---|
| 2026-09-14 | ✅ **구조 비교 60작업 완료(실패 0, 약 11시간).** hard(출력=물리식)·soft(물리=손실, 신규)·nn(물리 없음) × 타깃 min(센서 하한 −30℃ 급락 제거)·mean(1시간 평균) × 정제 5 × 시드 5. M: soft − hard ΔAUC +0.032(23/25)·리프트 +10.6, soft − nn −0.007(6/25) → **물리 기여는 정확도가 아니라 해석**. Z: hard 가 AUC 우위(+0.015 vs nn), GBM AUC 최고(0.924)지만 상위 1% 경보 적중은 soft·nn 93 vs GBM 77시간. 이상값 제거만으로 AUC +0.03~0.07. 권장 soft·KF. 문서 19 |
| 2026-09-14 | 학회 포스터 구성안(한국에너지기후변화학회 A0 양식 분석, 섹션별 초안, 설비 구조도·식 적용 위치, 유체·물성치·배관 정리). 시험구간 전체 빙결 시계열 그림(`timeline_plots.py`, 경보 예산 동일화). 문서 20 |
| 2026-09-13 | **학습 결함 2건 수정.** ① 표본 log ṁ 의 exp 가 float32 를 넘어 기울기 NaN → M·AE 시드 3/5 붕괴·Optuna trial 일부 AUC 0.5 → 식 ③ 을 로그공간 계산 + 비유한 배치 건너뛰기, 조합별 비교 전부 재실행. ② epoch 상한 400 은 손실만 더 줄 뿐 AUC 무관(1200 epoch 에서 0.881→0.879) → 유지. 물리식 상한 시험(`audit/`): 미래 상태를 완벽히 알아도 식 T61 시험 AUC 0.79/0.76 < 지속예보 0.87/0.86 |
| 2026-09-09 | `prex`/`trend` 전처리·EDA 완료 (HTR-31P 단일) |
| 2026-09-10 | 제작도서 OCR, 설계계수 확보 |
| 2026-09-11 | P&ID 판독, 정기점검 OCR, 정비 7건 자동탐지, AE/KF/LSTM 파일럿 |
| 2026-09-12 | **바이패스 발견** → 4대 확대, 관측 레이어 구축·검증, 물리모델 재작성, 유량 역산 |
| 2026-09-13 | ✅ **정제 비교 확정 — 시드 5회·짝비교(50회 학습).** 넣기 전 / ＋KF·＋AE·＋EKF 각각 / 3종 모두. M: 3종이 AUC +0.012·리프트5% +0.7·RMSE −0.10 (모두 5/5), 하나씩은 방향이 흔들림. Z: ＋KF 단독 리프트1% +2.6 (5/5), ＋EKF 는 리프트5% 0/5·μ_JT 0.542→0.493 으로 물리까지 흔듦. 문서 17 의 단일시드 "M 리프트1% 12.9→22.4" 는 잡음(시드평균 19.5→20.4) — 정정. 문서 18 |
| 2026-09-13 | ✅ **정제 3종(KF·AE·EKF) → 제한 PINN.** EKF 신규 — (Q5)(Q6) 비선형 관측모형으로 물리 이탈 혁신·결측 유량 추정(M 42k·Z 79k 시간 채움, NIS 0.41/0.24). 3종 모두 넣으면 M 리프트1% 12.9→22.4(GBM 22.8)·AUC 0.866(0.869), Z 리프트1% 31.8(GBM 29.9)·AUC 0.806(0.853). 단일 시드. 기본 REFINE=kf·ae·ekf. 문서 17 |
| 2026-09-13 | **2×2 검정 — 체제한정·열수지 특징 둘 다 기각.** 2016-07~ 만 학습하면 학습 사건이 3,681→225 로 줄어 AUC M 0.840→0.787 · Z 0.792→0.694, Brier 개선 56%→2%. 구 체제 사건도 빙결 물리 경로가 같아 학습 가치가 있다. 열수지 특징은 AUC −0.013/−0.014. 현행 설정 유지. 문서 15 §3 |
| 2026-09-13 | ✅ **제한 PINN 전 기간** — 성립이 확인된 (Q5) 줄-톰슨 + (Q6) 매설배관만 사용, N = U·A_배관/(m·c_p) + N₀ 에 열수지 유량. Z 는 N₀=0.040·μ_JT 0.546(공식 0.56)·U·A 8.71 kW/K 로 순수 식 성립, 성립 유량범위 M 1.3~3.0 · Z 2.6~8.6 kg/s. 빙결 AUC M 0.849(GBM 0.851) · Z 0.811(0.850). 데이터 한계 태그별 정리(문서 14), 2016 급감 원인은 운전 체제 변경(문서 15). 문서 16 |
| 2026-09-13 | **열화 추적 재도전** — 새 유량으로 `U·A = Q_gas/ΔT_lm` 을 정의대로 계산. 부하·계절 제거 후 A 만 −1.12%/년(대조군 p95 0.54%). 그러나 톱니 검정이 전부 음성(A p=0.45)이고 `TI33x`·`TI-D2x` 는 교정 프로그램에 없어 오염/드리프트 판별 불가. 문서 13 |
| 2026-09-13 | 🟢 **유량 경로가 열렸다.** 블로워 신호를 찾다가(없음) 수조가 열량계임을 이용 — 같은 사이클의 ON/OFF 대비로 `Q_gas`·`Q_loss` 를 상쇄시켜 유효 화력을 식별(A 0.54·B 0.49)하고 히터별 유량을 얻었다. 계절성 겨울/여름 M 2.91·Z 3.67, 직접 푼 혼합비와 +0.518(ε-NTU 는 −0.735). 문서 07 경로 5·6 정정, 문서 12 |
| 2026-09-13 | **검정 결과를 PINN 에 이식.** ε 을 역산이 아니라 **관측값**으로, 혼합가중 `w ∝ m_설계·ε`, β 는 밸브 단조맵. Z 는 세 지평 전부 GBM AUC 이상(0.982/0.954/0.939 vs 0.972/0.973/0.915). M 은 1시간 집계가 합류점식을 깨뜨려 (Q4) RMSE 9.1℃ 고착(1분에서는 3.34℃) → 1분 확장 필요. 문서 09 §8 |
| 2026-09-13 | **열전달식 검정 (PINN을 검정기로)** — 1분 원해상도 전기간, 대조군 12개. 합류점 혼합식은 성립(R² +0.52)하지만 **ε-NTU 유량 역산은 부호가 반대**(직접 푼 혼합비와 Spearman −0.735). ε≈0 은 대유량이 아니라 무유량이다. 문서 10 |
| 2026-09-13 | **히터 열전달식을 PINN 물리층에 투입** — `m·c_p·ΔT = U·A·ΔT_lm` ⇔ ε-NTU (실측 870만행에서 항등식 확인, 오차 1e-15). (Q2)(Q3)(Q4) 추가, (Q1) 절제실험으로 기각. M AUC 0.922/리프트 25.3, Z AUC 0.929/리프트 19.8. 문서 07 §4 정정 |
