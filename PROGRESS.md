# 진행 상황 (HTR-31P 가스히터 고장예측)

> 이 문서는 작업이 진행될 때마다 계속 업데이트한다. 최신 상태가 맨 위로 오도록 "현재 상태" 절만 덮어쓰고,
> 굵직한 마일스톤은 "타임라인"에 한 줄씩 추가한다.

## 현재 상태 (2026-09-11)

- **대상 설비**: HTR-31P(가스히터 P호기) 1대로 한정 (영종 공급관리소).
- **완료**: `prex/` 전처리, `trend/` EDA 시각화, `pid/` **P&ID 판독(신규)**.
- **문서**: `docs/htr31p_flow.md` — 흐름 다이어그램 + 태그별 신뢰도(✅/⚠️/❌). **§7에 P&ID 판독 결과 추가.**
- **🎉 오래 막혀 있던 미해결 2건이 P&ID로 해소됐다** (`docs/htr31p_flow.md` §7):
  - **`T_in` = `TI21Z` 단독 확정.** `TI21Y`는 인천도시가스(A/B) 계열 필터출구 헤더라 P호기와 무관하다.
    종전 `mean(TI21Y, TI21Z)`는 **다른 계열 온도를 섞고 있었다** → `prex/`·`trend/` 수정 완료.
  - **배관 위상 확정**: 히터 2대(31O·31P) 출구가 공통헤더 `400-NG-P7-358`로 합류한 뒤 정압기
    4라인(41O/P/Q/R)으로 분기. **1:1 대응이 아니다.**
- **⚠️ 대신 `gas_flow_proxy`의 근거가 무너졌다** (같은 판독의 결과):
  역산이 깔고 있던 "히터→정압기 직렬·무분기(질량보존)" 전제가 **반증**됐고,
  `PI43O`도 O/P/Q 공용헤더가 아니라 **`PCV-41O/42O` 유닛 전용 출구배관**의 값이었다(§7-3).
  → `gas_flow_proxy`는 "41P 라인이 공통헤더에서 뽑아 간 유량"일 뿐 P호기 히터 통과유량이 아니다.
  **H2식의 `m_gas`로 그대로 쓰면 안 된다.**
- **✅ `m_gas` 잠재변수 전환 설계 완료** → `docs/kfpinn_state_space.md` (상태공간 정식화 + **태그↔변수 매핑표**).
  - **대체 관측식 확보**: ε = (T_out−T_in)/(T_bath−T_in), NTU = −ln(1−ε) = U·A/(m_gas·c_p).
    **`PI43O`·`ZI41P`·`Cv`가 하나도 안 들어가고** 신뢰 태그 3개(TI21Z/TI33P/TI-D2P)만 쓴다.
    → `prex`에 `htx_eps`·`htx_ntu`·`htx_drive_c` 컬럼 신설(유효 5,021,531행 = 64.3%, ε 중앙값 0.244).
  - **식별성 진단 완료**(`prex/identify_mgas.py`): ε 단독으로는 U·A와 m_gas 분리 불가(구조적 미식별).
    버너 OFF 수조 열수지로 분리를 시도했으나 **실패**(m_gas 0.09배·U·A 0.06배·R² 0.38).
    원인은 **진공식 잠열항** — 비등 시 수조 유효 열용량이 현열값보다 커져 기울기·절편이 같은 배율로 나눠진다.
    (저부하 U·A 저하는 2.4배까지만 설명. 16배 괴리의 주범 아님.)
  - → **plug-in 추정 포기.** `m_gas`·`U·A`·`Q_burner`에 더해 **수조 유효 열용량 배율까지** 잠재변수로 두고
    필터로 공동추정한다. 열화지표는 **부하보정된 `UA_ref`**(U·A = UA_ref·(m/m_design)^0.8)의 하락으로 잡는다.
- **2025 ε 급등 추적** (`docs/htr31p_flow.md` §8) — ε 급등은 계기 아티팩트가 아니라 실제 값 변화였다.
  `PI-D2P`가 **2014-10-21 / 2024-11-01** 두 시점에 계단 이동하며 3구간으로 갈린다(기존 config 경계가
  2개월씩 틀려 있었음 → 수정). `vacuum_regime` 컬럼 신설.
  - ⚠️ **처음 해석("10년 진공 상실로 히터가 고장")은 과장이었고 철회했다**(§8-3).
    전체 행 중앙값으로 비교해 **가동률을 성능으로 오인**한 것. **버너 ON 구간만 보면 승온폭이
    shallow 20.47℃ vs deep-1 20.25℃로 동일**하고, 열화 추세도 없다. 히터는 멀쩡하다.
    → 2015 알람 인과 재해석·`PI-D2P_norm` 사용중단 권고도 함께 철회. 계기 스팬 가설이 오히려 유력.
  - ⚠️ **"일한 시간 552일" 수치도 틀려서 재정정**(§8-4): `H31POH`는 가동 기간이 아니라
    **버너 점화 사이클**(ON 중앙 15분)이라 승온폭과 AND로 묶으면 안 됐다. 승온폭>5℃ 단독 기준으로
    다시 세면 **2,535일(49.4%)**, 연 77~310일 — "연 6개월 가동" 감각과 맞는다. **표본량 문제 없음.**
  - ✅ **남는 진짜 이슈는 체제 불일치**(§8-5): deep-2(2024-11~)가 가동률 83.9%·승온 33℃·ε 0.696으로
    이전(42.9%·15.3℃·0.407) 대비 **2배 자주, 2배 세게** 돈다. 표본이 부족한 게 아니라
    **Test 구간의 운전 방식이 다른 것** → 분할 재설계 또는 체제를 모델 입력 조건으로 넣어야 한다.
- ✅ **정기점검 이력 파싱 완료(2026-09-11)** → `data/docs/_ocr/정기점검_이력.md` (H-31P 7건 + 정압기 1건 전건 OCR/파싱).
  **2024-10 정지(deep-2 전이)가 대규모 overhaul로 확정** — Fire/Smoke Tube 고압세척·설비개선 + Bundle Tube +
  연료정압기 분해정비(포펫·다이어프램 교체). → deep-2의 열교환 회복이 정비로 설명됨, `UA_ref`는 2024-10-30에서 리셋 필요.
  단 2014-10 전이는 그해 점검이 없어 확인 불가(계기이력 필요). `htr31p_flow.md §8-6` 갱신.
- **다음 단계**: (0) **학습/검증 구간 재설계**(§8-6, 2024-10-30을 기기상태 경계로),
  (1) `model/` 신설 — 상태공간 필터 구현(`prex`/`trend`/`pid`와 동일 구조),
  (2) 학습·검증 구간 분할 재검토(`04` §5-3), (3) 고장 라벨링(진짜 고장 vs. transient) — 이때 §7-4의
  `TSL_TSH_TSHH-31Z` → `SEQ.(HTR-31P)` 인터록을 `H31POH` 토글 해석에 활용, (4) `UA_ref(t)` ↔ 정기점검 이력 대조.

## 코드 구조 (파이프라인 방식)

```
prex/    원본 CSV → 전처리 Parquet (trend/alarm/fault 이벤트) + 파생지표(ε/NTU)
trend/   전처리 Parquet → EDA 그림(PNG)
pid/     P&ID .dwg → DXF/텍스트/PNG (배관 위상·태그 실물 위치 판독)
```

각 디렉토리는 `config.py`(설정) + 기능별 모듈 + `pipeline.py`(진입점) 구조를 공유한다.

### prex/ — 데이터 전처리 (완료)

```
.venv/bin/python -m prex.pipeline
```

- `config.py`: 대상 태그(TREND_TAGS), 알람 태그 분류(FAULT/STATUS/CONTROL), 경로.
- `trend.py`: Trend(1분 연속 시계열) 전처리 — 1분 그리드 reindex, 24시간 결측 시 segment 분리,
  PI-D2P era-wise 정규화(계기 스팬이 2015/2025년 전후로 바뀜).
- `alarm.py`: Alarm(DI/DO 이벤트) 전처리 — long 포맷 melt, 무효시각/중복 제거, 상승엣지 추출.
- `trend.py::add_heat_exchange()`: 열교환 효율 ε 과 NTU(=U·A/(m_gas·c_p)) 산출 — TI21Z/TI33P/TI-D2P
  **세 태그만** 쓰므로 P&ID로 흔들린 재료(PI43O/ZI41P/Cv)를 전부 우회한다. → `docs/kfpinn_state_space.md` §2.
- `identify_mgas.py`(진단): 밸브식 없이 m_gas·U·A 분리가 되는지 검증 → **안 된다**는 결론과 그 이유(진공 잠열항).
- `profiling.py`(진단, 2026-09-11 신설): 모델링 착수 전 기본 프로파일링 7종을 **표/CSV**로 낸다 —
  미리보기(head/tail) · 차원(shape+메모리) · 타입(dtype+결측률+고유값수) · 통계량(describe, p1/p99 포함) ·
  클래스분포(+불균형비) · 상관행렬(Pearson pairwise + Spearman) · 왜도(skew+첨도+판정).
  `trend/`가 같은 내용을 그림으로 보여주지만 수치를 못 읽고 dtype/왜도/불균형비는 아예 없어서 별도로 만들었다.
  실행 `.venv/bin/python -m prex.profiling` (`--quiet` 면 CSV만). 출력 `prex/output/profile/*.csv` 10개.
- `trend.py::estimate_gas_flow()`: 가스 질량유량(m_gas)이 Trend에 전량 NULL이라, 정압기(PCV-41P)
  밸브 유동식(문서 06 §3-1 G1)으로 역산 — `ZI41P`(개도)·`PI21X`(입구압)·`PI43O`(출구압) 사용.
  ⚠️ **2026-09-11 P&ID로 전제가 반증됨**: 히터↔정압기가 1:1이 아니라 공통헤더로 섞이고, `PI43O`는
  41O 유닛 전용값이다. 이 컬럼은 **P호기 히터 통과유량이 아니므로** H2식 `m_gas`로 쓰지 말 것
  (약한 부하지표로만) → `docs/htr31p_flow.md` §7-2·§7-3.
- 출력 (`prex/output/`, git 미포함):
  - `trend_htr31p.parquet` (7,804,924행): PI21X/TI21Y/TI21Z/TI33P/TI-D2P/PI-D2P/ZI41P/PI43O/RSF41P
    + segment_id + PI-D2P_era/PI-D2P_norm + gas_flow_proxy/ZI41P_frac/control_error_p
    + **htx_eps/htx_ntu/htx_drive_c**(2026-09-11 신설, 모델 입력의 핵심)
  - `identify_mgas_summary.csv`: 식별성 진단 연도별 결과(진단용)
  - `profile/*.csv` (10개): 프로파일링 결과(진단용)
  - `alarm_events_htr31p.parquet` (95,770행): 정제된 전체 DI/DO 이벤트
  - `fault_events_htr31p.parquet` (43,206행): 상승엣지(발생 시점)만 추출

### trend/ — EDA 시각화 (신규, 완료)

```
.venv/bin/python -m trend.pipeline
```

prex 출력을 입력으로 받아 일반적인 데이터 탐색 그림(상관관계/히트맵/박스플롯 등)을 생성한다.

- `config.py`: prex.config 재사용(태그 메타 중복 정의 안 함), 그림 스타일/샘플링 크기 설정.
- `style.py`: seaborn 테마 + 한글 폰트(Noto Sans CJK KR) 적용.
- `data.py`: parquet 로더 3종.
- `plots/overview.py`: 태그별 일평균 전체 기간 추이(line).
- `plots/distribution.py`: 히스토그램, 박스플롯, PI-D2P era별 박스플롯(원본 vs 정규화 비교).
- `plots/correlation.py`: 상관계수 히트맵, 산점도 행렬(pairplot, 5,000행 샘플링).
- `plots/missingness.py`: 태그별 월간 데이터 커버리지 히트맵.
- `plots/alarms.py`: 태그별 이벤트 발생 건수(symlog bar), 분류별 비중, FAULT 월별 발생 추이.
- 출력 (`trend/output/figures/`, git 미포함): `01_~10_*.png` 10종.

**의존성 추가**: matplotlib, seaborn을 `.venv`에 새로 설치. `requirements.txt`에 기록해 둠
(이전에는 pandas/numpy/pyarrow만 있었음).

### pid/ — P&ID 도면 판독 (신규, 완료)

```
.venv/bin/python -m pid.pipeline
```

`.dwg` 16장 중 P&ID 6장(`09-15-R-32-001~006`)을 열어 **배관 위상과 계기의 실제 설치 위치**를 판독한다.
원본이 AC1032(AutoCAD 2018) 바이너리라 그동안 이 환경에서 못 열었고, 그게 미해결 #3·#4의 근본 원인이었다.

- `config.py`: 도면 경로, 산출물 경로, 외부 도구 위치, 도면번호→내용 매핑(`SHEETS`).
- `convert.py`: ODA File Converter를 `xvfb-run`으로 헤드리스 호출해 DWG→DXF 변환(폴더 단위).
- `extract.py`: `ezdxf`로 TEXT/MTEXT/**INSERT의 ATTRIB**을 좌표째 추출(계기 태그는 대부분 ATTRIB이라
  TEXT만 읽으면 절반을 놓친다) + 판독용 PNG 렌더(원본이 검은 배경 기준이라 색 정책을 강제 반전).
- 출력 (`data/.../260410 영종GS 도면/_converted/`, git 미포함):
  `dxf/` 6장, `png/` 전체도 6장 + `판독_*.png` 9장, `pid_text.json`(텍스트 5,327건 + 좌표).

**최초 1회 시스템 설치 필요** (파이썬 모듈만으로는 DWG를 못 연다):

```
yay -S oda-file-converter          # AUR
sudo pacman -S xorg-server-xvfb    # GUI 앱이라 헤드리스 실행용
```

**의존성 추가**: `ezdxf==1.4.4` (`requirements.txt` 반영).

## EDA 결과에서 눈에 띈 점 (다음 단계 참고용)

- STATUS 태그(특히 `H31POH`, 가동상태 토글)가 FAULT 태그보다 발생 건수가 수십~수천 배 많아
  단순 빈도 기준으로 보면 안 됨 (→ 알람 카운트 그림은 symlog 스케일 사용).
- FAULT 이벤트는 2014~2018년 사이에 집중 발생(특히 2015년 초 스파이크)하고 이후 낮은 수준 유지.
  라벨링 단계에서 이 시기 데이터 품질/원인을 별도로 확인할 필요.
- ~~TI21Y/TI21Z(히터 입구온도)는 상관계수 0.87로 거의 중복 정보~~ → **(2026-09-11 정정)** 중복이 아니라
  **서로 다른 수요처 계열의 헤더온도**다. TI21Z=O/P(P호기), TI21Y=A/B(도시가스). 0.87은 같은 부지의
  동일 기후 때문이지 이중화라서가 아니다. **모델 입력은 TI21Z 단독.**
- PI-D2P는 era(2011-2014 / 2015-2024 / 2025-)별로 값 분포가 확연히 다름 → `PI-D2P_norm`
  (era-wise z-score) 컬럼을 원본 대신 사용 권장.
- 데이터 커버리지는 대체로 양호하나 2018-10, 2023-02~09, 2024-05~08, 2026-05~ 부근에 결측 구간 존재.

### 프로파일링에서 새로 드러난 점 (2026-09-11, `prex/profiling.py`)

- **`htx_eps` ↔ `htx_ntu` Spearman = 1.000** — NTU=−ln(1−ε)는 ε의 단조변환이라 당연하다.
  **모델 입력에 둘 다 넣지 말 것** (물리식에는 NTU, 관측에는 ε 하나만).
- **`gas_flow_proxy` ↔ `ZI41P_frac` Pearson = 0.984** — proxy가 사실상 밸브 개도의 재표현이다.
  이미 약한 부하지표로 강등했지만(§7-2), 개도와 거의 중복이라는 근거가 하나 더 생겼다.
- **라벨 불균형이 극심하다**: `fault_events.tag` 불균형비 **36,296:1** (`H31POH` 혼자 84.0%),
  category 기준으로도 STATUS 86.5% / FAULT 13.5% / CONTROL 0.009%.
  → 문서 04 §4-3의 "단순 이진분류 불가, 잔차 기반 비지도 이상탐지가 맞다"가 수치로 확인됐다.
- **왜도가 큰 열**: `RSF41P`(skew −8.80, 첨도 365 — 설정압이라 거의 상수 + 드문 계단 변경),
  `PI21X`(−4.48), `ZI41P`(+3.22), `control_error_p`(+2.02). 필터 입력 전 변환/스케일링 검토 대상.
  `PI43O`는 skew 0.54로 대칭이나 **첨도 19.9**로 꼬리가 두껍다(계기 dropout 추정).
- **`htx_drive_c` 최소 −50℃** — 수조가 가스 입구보다 차가운 시각이 존재한다(정지·점검 중 추정).
  ε 계산에서 `EPS_MIN_DRIVE_C=15℃` 필터가 왜 필요한지 보여주는 수치다.

## 타임라인

- 2026-09-09: `prex/` 전처리 파이프라인 완료(HTR-31P 1대 한정 결정 포함). `trend/` EDA 시각화
  파이프라인 신규 작성 및 검증 완료.
- 2026-09-10: 6개 태그만으로 고장예측이 충분한지 검토 → 가스 질량유량(m_gas) 완전 결측 확인.
  정압기 밸브식으로 `gas_flow_proxy` 역산해 `prex/`에 추가. HTR-31P 가스 흐름/태그 신뢰도를
  정리한 `docs/htr31p_flow.md` 작성 — TI21Y/TI21Z 입구 태그 모호성, 히터↔정압기 배관 위상
  미확정(P&ID 필요), Q_burner 실측 없음을 미해결 항목으로 명시.
- 2026-09-11: **P&ID 6장 판독으로 미해결 #3·#4 해소** (`pid/` 파이프라인 신규). `T_in`을 `TI21Z` 단독으로
  확정·코드 반영(구 평균 방식은 인천도시가스 계열 온도 혼입). 히터↔정압기가 공통헤더로 섞이는 것이
  확인되어 **`gas_flow_proxy`의 직렬 가정이 반증**됨 → `m_gas` 잠재변수 전환이 다음 과제로 확정.
  `PI43O`가 공용헤더가 아니라 41O 유닛 전용값임도 정정. 상세: `docs/htr31p_flow.md` §7.
- 2026-09-11(2): **`m_gas` 잠재변수 전환 설계 확정** — `docs/kfpinn_state_space.md` 신설(상태공간 6변수 +
  태그↔변수 매핑표). ε-NTU 관측식으로 밸브식 의존을 제거하고, 식별성 진단으로 plug-in 추정이 불가함을
  (진공 잠열항 때문) 데이터로 확인. 열화지표를 **부하보정 `UA_ref`**로 재정의.
- 2026-09-11(3): `prex/profiling.py` 신설 — 모델링 착수 전 기본 프로파일링 7종(미리보기·차원·타입·통계량·
  클래스분포·상관·왜도)을 표/CSV로 산출. 라벨 불균형 36,296:1, `htx_eps`↔`htx_ntu` 완전 단조중복 등 확인.
- 2026-09-11(4): `trend/` 확장(ANALYSIS_TAGS 16열 상관·결측, ε/NTU 그림 3종 신설) → 그 그림에서
  **2025 ε 급등** 발견·추적(`docs/htr31p_flow.md` §8). `PI-D2P` 계단 이동 시점을 2014-10-21/2024-11-01로
  특정해 era 경계 2개월 오차 수정, `vacuum_regime` 컬럼 신설.
- 2026-09-11(5): **정기점검 8건 OCR/파싱 완료** → `data/docs/_ocr/정기점검_이력.md` 신설(연도별 정비내역 통합표).
  스캔 PDF(13/15/17/19년, 25년 PCV)는 tesseract, xls/xlsx(11/21/24년)는 xlrd/openpyxl로 파싱해 원문을 `_ocr/*.txt`로
  보존. **2024-10 정지=대규모 overhaul 확정**(deep-2 원인 규명, §8-6). `_ocr/README.md` 매니페스트 갱신
  (점검 8건·P&ID 완료 반영, 지난 ".dwg 보류" 항목 정정).
  ⚠️ 1차 해석("10년 진공 상실=고장")은 **가동률을 성능으로 오인한 과장이라 같은 날 철회**(§8-3).
  결론: 히터 성능·열화 이상 없음. **실제 문제는 표본량** — 15년 중 히터가 일한 시간 552일(10.2%),
  그중 35%가 최근 22개월 집중 → 학습/검증 분할을 '일한 시간' 기준으로 재설계 필요(§8-4·§8-5).
